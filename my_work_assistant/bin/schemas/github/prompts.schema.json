{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Prompt Metadata",
  "type": "object",
  "properties": {
    "managed_by_mcp": {"const": true},
    "version": {"const": 1},
    "topic": {"type": "string", "enum": ["document-api", "review-code", "onboarding-plan"]}
  },
  "required": ["managed_by_mcp", "version", "topic"]
}
